[project]
name = "kernelforge-nki-mcp"
version = "0.1.0"
description = "MCP server for the Kernel Forge NKI optimization agent (PyTorch / Triton -> NKI on AWS Trainium)."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT-0" }
dependencies = [
    "mcp>=1.0.0,<2",
    "boto3>=1.34.0",
    "httpx>=0.27.0",
    "pydantic>=2.6.0",
]

[dependency-groups]
# `uv run` reads [dependency-groups], not [project.optional-dependencies], so the
# dev tooling has to live here or `uv run ruff`/`uv run pytest` fail to spawn.
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.5",
    "pip-audit>=2.7",
]

[project.scripts]
kernelforge-nki-mcp = "kernelforge_nki_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kernelforge_nki_mcp"]
