[pytest]
# Run the whole suite from the repo root with a bare `pytest`.
# conftest.py wires up the per-package import paths (mcp/src, infrastructure/agentcore, repo root).
testpaths =
    infrastructure/reward_server/tests
    infrastructure/agentcore/tests
    mcp/tests
    tests
addopts = -q
